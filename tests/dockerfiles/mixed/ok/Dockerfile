# The leg of D5 that COMPLETES. Pinned to the same base as
# tests/dockerfiles/good so the two stay comparable, and deliberately trivial:
# D5 is about what the summary reports, not about what this image contains.
FROM alpine:3.18
RUN echo "d5-ok: this leg builds and scans"
